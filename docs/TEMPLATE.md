# <Title>

> One-sentence hook: what this room holds and when to come here.

## Purpose

Why this exists / what problem it solves. The *why*, not a feature list.

## Key files

- `path/to/file.php` — what it does.
- `path/to/other.tsx` — what it does.

## How it works

The mechanism, in the order a reader would trace it. Link to code rather than
pasting large blocks; short illustrative snippets are fine.

## Decisions & why

The non-obvious choices and their rationale (or link to the relevant
`docs/decisions/NNNN-*.md` ADR). What we deliberately did *not* do.

## Gotchas

Footguns, edge cases, ordering constraints, "if you change X also change Y".

## Related

- [`docs/...`](../...) — sibling rooms.
- `CLAUDE.md` § <section> — the quick-ref version.
